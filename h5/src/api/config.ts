/**
 * 全局配置（H5 跨端）——好好吃饭
 * baseURL：后端 REST 基址。
 * 当前用服务器 IP（域名 nianmu.top 待 ICP 备案通过后再切换，未备案会被阿里云 403 拦截）。
 */
const API_BASE = 'http://127.0.0.1:8000/api/v1'

export default {
  apiBase: API_BASE
}
